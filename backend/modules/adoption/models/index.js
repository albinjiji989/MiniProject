/**
 * Adoption Models Index
 * Centralized export for all adoption-related models
 */

const UserPetInteraction = require('./UserPetInteraction');
const ModelPerformance = require('./ModelPerformance');

module.exports = {
  UserPetInteraction,
  ModelPerformance
};
